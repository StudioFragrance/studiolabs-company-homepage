# Docker 환경 설정
NODE_ENV=production

# 데이터베이스 설정 (Docker Compose 네트워크)
DATABASE_URL=postgresql://postgres:postgres123@postgres:5432/studiofragrance

# 서버 설정
PORT=5000

# 세션 보안 설정 (프로덕션용 강화된 키로 교체 필요)
SESSION_SECRET=your-super-secure-session-secret-change-this-in-production

# OAuth 설정 (실제 값으로 교체 필요)
NAVER_WORKS_CLIENT_ID=your-client-id
NAVER_WORKS_CLIENT_SECRET=your-client-secret
NAVER_WORKS_DOMAIN_ID=your-domain-id

# Docker 환경에서는 localhost 대신 실제 도메인 사용
NAVER_WORKS_REDIRECT_URI=http://localhost:5000/auth/naver-works/callback