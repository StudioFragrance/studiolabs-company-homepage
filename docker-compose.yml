version: '3.8'

services:
  # PostgreSQL 데이터베이스 서비스
  postgres:
    image: postgres:17
    container_name: studiolabs_homepage_db
    env_file:
      - .env.docker
    ports:
      - "5430:5430"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U studiolabs -d studiolabs"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 웹 애플리케이션 서비스
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: studiolabs-homepage
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://studiolabs:VdPyLLSTNf7jTsH@postgres:5430/studiolabs
    ports:
      - "5000:5000"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - app-network
    restart: unless-stopped

# 공유 네트워크 정의
networks:
  app-network:
    driver: bridge
    name: studiolabs_homepage_network

# 볼륨 정의
volumes:
  postgres_data:
    driver: local
    name: studiolabs_homepage_postgres_data