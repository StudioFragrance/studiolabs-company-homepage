# 이 파일은 .env 파일의 템플릿입니다.
# 실제 값으로 채운 뒤 .env 파일로 복사하여 사용하세요.
# .env 파일은 절대로 Git에 커밋하면 안 됩니다.

# 환경 설정 (development, production 등)
NODE_ENV=development

# 데이터베이스 설정
# 형식: postgresql://사용자이름:비밀번호@호스트:포트/데이터베이스이름
DATABASE_URL=postgresql://your_db_user:your_db_password@localhost:5430/studiofragrance

# 서버 설정
PORT=5000

# OAuth 설정 (네이버웍스 개발자 콘솔에서 발급받은 값으로 교체)
NAVER_WORKS_CLIENT_ID="your_naver_works_client_id"
NAVER_WORKS_CLIENT_SECRET="your_naver_works_client_secret"
NAVER_WORKS_REDIRECT_URI=http://localhost:5000/auth/naver-works/callback

# 세션 시크릿 (아래 명령어로 강력한 시크릿 생성 가능)
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET="generate_a_strong_random_secret_string_here"